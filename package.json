{
  "name": "disc-studio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --config source/concept-b/vite.config.ts",
    "preview": "vite preview",
    "smoke": "npm run build && node scripts/smoke.mjs"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "svelte": "^5.38.0",
    "vite": "^7.1.4"
  }
}
